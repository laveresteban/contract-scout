# --- Web framework / async stack -------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
aiosqlite>=0.20
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
alembic>=1.13
python-dotenv>=1.0

# --- Auth (OAuth + JWT sessions) -------------------------------------------
PyJWT>=2.9
Authlib>=1.3
itsdangerous>=2.2

# --- Scraping sources -------------------------------------------------------
python-jobspy==1.1.82   # Indeed/LinkedIn/Glassdoor/ZipRecruiter/Google major boards
pandas>=2.2             # jobspy returns DataFrames
playwright>=1.47        # headless-render fallback (Dice etc.); optional at runtime
apify-client>=1.7       # Apify remote-jobs feed; optional at runtime
aiofiles>=24.1

# --- Scheduling -------------------------------------------------------------
APScheduler>=3.10

# --- dev / test -------------------------------------------------------------
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21
